# Financial Research Agent Configuration Template
# Copy this to .env and fill in your actual values
# IMPORTANT: Never commit .env to git!

# ===== LLM Configuration =====
# All agents use OpenAI models optimized for cost/quality balance
# Current cost: ~$0.50/report (see COST_OPTIMIZATION_ANALYSIS.md for optimization strategies)

# ===== LLM API Keys =====
OPENAI_API_KEY=your_openai_api_key_here

# Brave Search API (for web searches)
BRAVE_API_KEY=your_brave_api_key_here

# Serper Search API (Google results - fallback for Brave)
SERPER_API_KEY=your_serper_api_key_here

# ===== SEC EDGAR Integration =====
# Enable SEC EDGAR MCP integration (true/false)
ENABLE_EDGAR_INTEGRATION=true

# REQUIRED for SEC EDGAR: User-Agent header
# Format: "CompanyName/Version (contact@email.com)"
# See: https://www.sec.gov/os/accessing-edgar-data
SEC_EDGAR_USER_AGENT=YourCompany/1.0 (your-email@example.com)

# EDGAR MCP Server Command (use python -m for installed package)
EDGAR_MCP_COMMAND=/path/to/.venv/bin/python

# EDGAR MCP Server Arguments (run the MCP server module)
EDGAR_MCP_ARGS=-m sec_edgar_mcp.server

# ===== Supabase Configuration (optional - for web app authentication) =====
# SUPABASE_URL=https://your-project.supabase.co
# SUPABASE_ANON_KEY=your_anon_key_here
# AUTH_REDIRECT_URL=http://localhost:7860/auth/callback

# ===== Agent Model Configuration =====
# Model assignments are defined in config.py
# Current configuration uses GPT-4.1, GPT-5, and o3-mini models
# For cost optimization strategies, see COST_OPTIMIZATION_ANALYSIS.md
#
# You can override individual agent models here:
# PLANNER_MODEL=o3-mini
# SEARCH_MODEL=gpt-4.1
# WRITER_MODEL=gpt-5.1
# VERIFIER_MODEL=gpt-4.1
# EDGAR_MODEL=gpt-4.1
# FINANCIALS_MODEL=gpt-5
# RISK_MODEL=gpt-5
# METRICS_MODEL=gpt-5
#
# Reasoning effort levels (for GPT-5/o3 models): minimal, low, medium, high
# Note: o3-mini only supports "low", "medium", "high" (not "minimal")
# PLANNER_REASONING_EFFORT=low
# SEARCH_REASONING_EFFORT=minimal
# WRITER_REASONING_EFFORT=low
# VERIFIER_REASONING_EFFORT=minimal
# EDGAR_REASONING_EFFORT=minimal
# FINANCIALS_REASONING_EFFORT=low
# RISK_REASONING_EFFORT=low
# METRICS_REASONING_EFFORT=minimal

# ===== Search Configuration =====
MAX_SEARCH_RETRIES=3
SEARCH_RETRY_DELAY=2.0

# ===== Agent Execution Configuration =====
# Maximum number of tool calls an agent can make in a single run
# Default: 25 (SDK default is 10, which is too low for financial metrics agent)
# Financial metrics agent may need 15-20 turns to extract statements and calculate all ratios
MAX_AGENT_TURNS=25

# ===== Output Configuration =====
OUTPUT_DIR=financial_research_agent/output
