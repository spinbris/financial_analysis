# Financial Research Agent Configuration
# Copy this file to .env and update with your values

# ============================================================================
# REQUIRED: SEC EDGAR User Agent
# ============================================================================
# Format: "YourCompanyName/Version (your-email@example.com)"
# Required by SEC.gov to identify your application
# See: https://www.sec.gov/os/accessing-edgar-data
SEC_EDGAR_USER_AGENT="FinancialResearchAgent/1.0 (your-email@example.com)"

# ============================================================================
# Feature Flags
# ============================================================================
# Enable SEC EDGAR integration for official financial data
ENABLE_EDGAR_INTEGRATION=true

# ============================================================================
# Model Configuration (Performance Optimized)
# ============================================================================
# Use gpt-4o-mini for simple tasks, gpt-4o for critical analysis
#
# IMPORTANT: GPT-5 API models are REASONING models (very slow - 15-20 min!)
# For speed, stick with GPT-4o which is optimized for latency
#
# GPT-4o Pricing (2025):
#   gpt-4o:      $2.50/$10 per 1M input/output tokens
#   gpt-4o-mini: $0.15/$0.60 per 1M input/output tokens

# Planning Agent - Use gpt-4o-mini for search strategy (fast & cheap)
PLANNER_MODEL=gpt-4o-mini
PLANNER_REASONING_EFFORT=minimal

# Search Agent - Use gpt-4o-mini for web search summarization (fast)
SEARCH_MODEL=gpt-4o-mini
SEARCH_REASONING_EFFORT=minimal

# EDGAR Agent - Use gpt-4o for SEC filing analysis (fast & high quality)
EDGAR_MODEL=gpt-4o
EDGAR_REASONING_EFFORT=minimal

# Financial Metrics Agent - Use gpt-4o for financial calculations
METRICS_MODEL=gpt-4o
METRICS_REASONING_EFFORT=minimal

# Financial Analysis Agent - Use gpt-4o for comprehensive analysis
FINANCIALS_MODEL=gpt-4o
FINANCIALS_REASONING_EFFORT=low

# Risk Analysis Agent - Use gpt-4o for risk assessment
RISK_MODEL=gpt-4o
RISK_REASONING_EFFORT=low

# Writer Agent - Use gpt-4o for final report synthesis
WRITER_MODEL=gpt-4o
WRITER_REASONING_EFFORT=low

# Verifier Agent - Use gpt-4o for accuracy verification
VERIFIER_MODEL=gpt-4o
VERIFIER_REASONING_EFFORT=minimal

# ============================================================================
# Agent Execution Configuration
# ============================================================================
# Maximum tool calls per agent (increase if agents hit limits)
MAX_AGENT_TURNS=25

# Search retry configuration
MAX_SEARCH_RETRIES=3
SEARCH_RETRY_DELAY=2.0

# ============================================================================
# Output Configuration
# ============================================================================
OUTPUT_DIR=financial_research_agent/output

# ============================================================================
# Performance Notes
# ============================================================================
# Recommended models by use case:
#
# For SPEED & QUALITY (5-7 min total) - RECOMMENDED:
#   PLANNER_MODEL=gpt-4o-mini
#   SEARCH_MODEL=gpt-4o-mini
#   All others=gpt-4o
#   Cost: ~$0.15 per report
#   ✅ Optimal balance of speed and quality
#
# For SPEED & COST SAVINGS (5-8 min total) - TEST THIS:
#   PLANNER_MODEL=gpt-5-nano
#   SEARCH_MODEL=gpt-5-nano
#   All others=gpt-5
#   All REASONING_EFFORT=minimal (for simple tasks)
#   FINANCIALS/RISK/WRITER_REASONING_EFFORT=low (for analysis)
#   Cost: ~$0.08 per report (47% cheaper!)
#   ⚡ GPT-5 with minimal reasoning can be fast!
#
# For MAXIMUM SPEED (3-5 min total):
#   ALL MODELS=gpt-4o-mini
#   Cost: ~$0.05 per report
#   ⚠️ Lower quality on complex analysis
#
# For DEEP REASONING (15-20 min total):
#   All models=gpt-5
#   All REASONING_EFFORT=medium or high
#   Cost: ~$0.08 per report
#   ⚠️ Very slow - only for complex reasoning tasks
#
# REASONING_EFFORT levels:
#   GPT-5 models (gpt-5, gpt-5-mini, gpt-5-nano):
#     minimal = Very fast, 23x fewer tokens, good quality (score 44)
#     low     = Fast, better quality (score 64)
#     medium  = Slow, great quality (score 67) - default
#     high    = Very slow, best quality (score 68)
#
#   o1/o3 models (o1, o1-mini, o3, o3-mini):
#     low     = Fastest option (minimal not supported)
#     medium  = Balanced (default)
#     high    = Slowest, highest quality
