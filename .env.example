# Financial Research Agent Configuration
# Copy this file to financial_research_agent/.env and update with your values

# ============================================================================
# REQUIRED: API Keys
# ============================================================================
# OpenAI API Key (required)
OPENAI_API_KEY=sk-proj-YOUR_KEY_HERE

# Brave Search API (recommended - 10x cheaper than OpenAI WebSearchTool)
# Get your free key at: https://search.brave.com/search/api
BRAVE_API_KEY=BSA_YOUR_KEY_HERE

# Groq API (optional - for LLM_PROVIDER=groq)
# Get your free key at: https://console.groq.com/keys
GROQ_API_KEY=gsk_YOUR_KEY_HERE

# Serper Search API (optional - Google results fallback)
SERPER_API_KEY=YOUR_KEY_HERE

# ============================================================================
# REQUIRED: SEC EDGAR User Agent
# ============================================================================
# Format: "YourCompanyName/Version (your-email@example.com)"
# Required by SEC.gov to identify your application
# See: https://www.sec.gov/os/accessing-edgar-data
SEC_EDGAR_USER_AGENT=FinancialResearchAgent/1.0 (your-email@example.com)

# ============================================================================
# LLM Provider Selection
# ============================================================================
# Options: "openai" (default, highest quality) or "groq" (faster, cheaper)
# Note: Groq requires specific models and may have compatibility issues
LLM_PROVIDER=openai

# ============================================================================
# Cost Mode Selection (NEW - Simplified Configuration)
# ============================================================================
# Options: "standard" (higher quality, ~$0.80/report) or "budget" (cheaper, ~$0.30/report)
#
# Standard mode uses:
#   - gpt-4.1 for: Search, Verifier, EDGAR
#   - gpt-5 for: Financials, Risk, Metrics
#   - gpt-5.1 for: Writer
#   - o3-mini for: Planner
#
# Budget mode uses:
#   - gpt-4o-mini for: Search, Verifier, EDGAR
#   - gpt-5-nano for: Financials, Metrics
#   - gpt-5.1 for: Writer, Risk
#   - o3-mini for: Planner
#
COST_MODE=standard

# ============================================================================
# Feature Flags
# ============================================================================
# Enable SEC EDGAR integration for official financial data
ENABLE_EDGAR_INTEGRATION=true

# EDGAR MCP Server Configuration
EDGAR_MCP_COMMAND=/path/to/.venv/bin/python
EDGAR_MCP_ARGS=-m sec_edgar_mcp.server

# ============================================================================
# Agent Execution Configuration
# ============================================================================
# Maximum tool calls per agent (increase if agents hit limits)
MAX_AGENT_TURNS=25

# Search retry configuration
MAX_SEARCH_RETRIES=3
SEARCH_RETRY_DELAY=2.0

# ============================================================================
# Output Configuration
# ============================================================================
OUTPUT_DIR=financial_research_agent/output

# ============================================================================
# Advanced: Override Individual Models (Optional)
# ============================================================================
# You can override specific models if needed. These override COST_MODE defaults.
# Uncomment and modify as needed:
#
# PLANNER_MODEL=o3-mini
# SEARCH_MODEL=gpt-4.1
# WRITER_MODEL=gpt-5.1
# VERIFIER_MODEL=gpt-4.1
# EDGAR_MODEL=gpt-4.1
# FINANCIALS_MODEL=gpt-5
# RISK_MODEL=gpt-5
# METRICS_MODEL=gpt-5
#
# Reasoning effort per model (minimal, low, medium, high)
# Only applies to GPT-5 and o3 models
#
# PLANNER_REASONING_EFFORT=minimal
# SEARCH_REASONING_EFFORT=minimal
# EDGAR_REASONING_EFFORT=minimal
# METRICS_REASONING_EFFORT=minimal
# FINANCIALS_REASONING_EFFORT=low
# RISK_REASONING_EFFORT=low
# WRITER_REASONING_EFFORT=low
# VERIFIER_REASONING_EFFORT=minimal

# ============================================================================
# Cost Comparison
# ============================================================================
# Standard mode: ~$0.80 per report (gpt-4.1, gpt-5 - best quality)
# Budget mode:   ~$0.30 per report (gpt-4o-mini, gpt-5-nano - 62% savings)
# Groq mode:     ~$0.15 per report (10-100x faster, experimental)
#
# Reports per $20:
#   Standard: ~25 reports
#   Budget:   ~66 reports
#   Groq:    ~133 reports
